## Legacy Apps Archive

This folder contains archived standalone apps that were used before the unified Waterpolo Hub:

- `waterpolo-shotmap`
- `waterpolo-analytics`

Notes:
- Source files are preserved.
- `node_modules` and local Finder metadata files were removed as safe cleanup.
- These apps are not used by the current main app in `src/`.
